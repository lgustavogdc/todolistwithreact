import React from 'react';

import './Header.css'

const header = () =>{
    return(
        <header className="main-header">
            <h1 className="h1-header">ToDo-List</h1>
        </header>
    )
}

export default header;